현재 버전: **0.9.4**. 새 설정과 업데이트 안내는 [README.md](README.md)를 참고하세요.

# 설치와 사용법 — 한지미 0.8.13

## 자동 설치

1. ZIP을 새 폴더에 풉니다. 구버전 파일 위에 덮어 풀지 마세요.
2. 전체 Xcode와 Python 3가 필요합니다. Xcode를 한 번 실행해 약관과 필수 구성 요소 설치를 완료하세요. macOS 27에서는 호환되는 Xcode 27이 필요합니다.
3. 잠시 ABC로 전환하고 `repair_and_install.command`를 실행합니다. 관리자 권한으로 실행하지 마세요. 검증·등록 후 실제 앱은 `~/Library/Input Methods/HanjaIME.app`에 설치됩니다. 기존 한지미는 복구용으로 보관합니다.
4. 시스템 설정 → 키보드 → 텍스트 입력 → 편집에서 HanjaIME 두벌식을 추가합니다. 항목이 나타나지 않으면 로그아웃 후 로그인합니다.
5. 입력 메뉴에서 **HanjaIME 두벌식**을 선택합니다. 영어는 ABC로 전환해 입력합니다.

공식 구름과 다른 입력기는 유지됩니다. **입력 메뉴 → HanjaIME 설정… → 개인 단어**에서 단어를 관리합니다. `open_hanjaime_settings.command`로 설정을 바로 열 수도 있습니다. 기존 개인 단어 데이터는 유지하며, 자동 설치는 설치된 구버전 관리 앱을 대체하고 그 앱을 가리키던 `~/Applications`와 `/Applications`의 정확히 일치하는 바로가기만 정리합니다. 관계없는 파일은 삭제하지 않습니다.

설치가 끝나면 다운로드한 원본 폴더를 지워도 설치된 입력기는 동작해야 합니다. 다만 재설치·복구를 위해 ZIP 보관을 권합니다.

## Xcode 없는 수동 설치

이 ZIP의 `dist/HanjaIME.app`은 미리 빌드된 Apple Silicon용 앱입니다. ABC로 전환하고 기존 앱을 별도로 백업한 뒤 Finder의 이동 → 폴더로 이동에서 `~/Library/Input Methods`를 열어 복사합니다. 로그아웃 후 로그인하고 위의 입력기 추가 절차를 진행합니다. 개인 단어 관리는 입력 메뉴의 HanjaIME 설정에서 엽니다. 구 관리 앱 바로가기 정리는 자동 설치를 사용하세요. 자동 설치가 제공하는 등록 정리와 진단은 수동 복사에서는 실행되지 않습니다.

현재 ad-hoc 서명이고 Apple 공증은 없습니다. 출처를 확인하고 macOS가 허용하는 개별 앱 승인 절차를 사용하세요. 시스템 보안 기능 전체를 해제하지 마세요. 다른 Mac에서의 최초 설치 검증은 별도로 필요합니다.

## 입력과 변환

- 한글을 입력하면 한자 후보창이 자동으로 나타납니다.
- Space: 변환 후보 선택/전환. Enter: 현재 내용 확정.
- Shift+Space: 한글을 유지하면서 공백 입력. Esc: 변환 취소.
- 후보창의 방향키·번호·페이지 이동으로 다른 후보를 고를 수 있습니다.
- 분류에서 표준·한자·이름·신자체·기호를 선택합니다. 창이 좁으면 아래 드롭다운을 사용합니다.
- 기호 분류에서 미국→🇺🇸🗽🏈, 영국→🇬🇧💷💂 및 관련 국가, 생일→🎂🎉🎁 등을 확인할 수 있습니다.
- 덴마크→丁抹/丹麥, 하늘→天, 바다→海처럼 등록된 단어는 의미로도 검색됩니다. 음역·중국어 표기·뜻 연결은 설명으로 구분합니다.
- 책 모양 버튼은 뜻풀이를 엽니다. 설정 → 개인 단어에서 단어를 등록·수정·삭제·백업·가져오기 할 수 있습니다. 신규 설치에는 제작자 개인 단어가 없으며 기존 사용자 사전을 삭제하지 않습니다.

## 점검·복구

TextEdit, Safari 주소창, Spotlight, Finder 검색, ChatGPT 입력란에서 첫 단어 변환 후 두 번째 단어 후보창을 확인하세요. 두벌식 선택 중 영문이 나오면 입력 메뉴 상태·Caps Lock 상태·발생 앱·재현 순서를 기록해주세요. 암호 입력칸 등 보안 입력 환경은 별도 제약이 있을 수 있습니다.

`check_environment.command`, `verify_input_sources.command`, `diagnose_input_sources.command`로 진단할 수 있습니다. 생성되는 로그에는 로컬 경로가 포함될 수 있으므로 공개 전에 검토하세요. 로그는 이 배포 ZIP에 포함되어 있지 않습니다.

제거: `uninstall_hanjaime.command`. 이전 설치 복구: `restore_hanjaime.command`. 개인 단어를 내보내 보관한 뒤 작업하세요.

## 새 설정과 변경 내역

설정은 일반·입력·개인 단어·사전·정보·라이선스 탭으로 나뉩니다. 창 크기를 조절하거나 내용을 스크롤할 수 있습니다. 정보 → 변경 내역 보기를 누르면 새로운 기능을 소개하는 별도 창이 열립니다.

## 네이버 사전 연결

후보의 뜻풀이를 열면 Apple 사전을 먼저 조회합니다. Apple 사전에 결과가 없으면 네이버 공식 사전 페이지를 창 안에 표시합니다. 일본어 신자체는 일본어사전, 나머지 한자는 한자사전으로 연결합니다. 인터넷 연결이 필요합니다. 결과가 없을 수 있으며 연결 실패 시 ‘브라우저에서 열기’를 사용할 수 있습니다.

설정 → 사전 → ‘Apple 사전에 결과가 없으면 네이버 사전 자동 연결’로 기능을 끌 수 있습니다. 켜져 있을 때 뜻풀이를 연 후보의 검색어가 네이버에 전송됩니다. 작성 중인 전체 문장·개인 단어 목록을 보내지 않습니다. 내장 웹뷰는 영구 쿠키 저장소를 사용하지 않으며 사전 페이지 내용을 추출해 배포하거나 사전 데이터베이스로 저장하지 않습니다. 웹사이트 자체의 통신과 개인정보 처리는 해당 제공자의 정책을 따릅니다.
