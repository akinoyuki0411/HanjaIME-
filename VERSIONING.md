# 버전 규칙

현재 버전은 **0.9.4**입니다. 기존 0.8.14에서 배포 번호를 새 기준으로 정리했습니다.

- 세 부분은 `큰 버전.가운데 버전.작은 버전`입니다.
- 마지막 숫자는 0~9입니다: `0.9.4 → 0.9.5 → … → 0.9.9 → 0.10.0`.
- 예: `0.0.9 → 0.1.0`. 가운데 숫자까지 한 자리로 제한하지 않습니다.
- 새 번호 계산: `python3 scripts/next_version.py 0.9.9`.
- 기존 0.8.14 사용자가 업데이트할 수 있도록 앱의 버전 비교기는 과거의 두 자리 마지막 숫자도 읽습니다. 새 릴리스 번호 규칙과 과거 버전 해석은 별개입니다.
- 공개 버전, 앱 정보, 설치기, 태그와 배포 ZIP 이름을 맞춥니다. 이미 공개한 태그와 파일은 덮어쓰지 않습니다.
